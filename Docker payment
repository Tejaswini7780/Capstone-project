# Use Python base image
FROM python:3.10-slim

# Set working directory
WORKDIR /app

# Copy files
COPY . .

# Install dependencies
RUN pip install flask requests

# Expose port (change for each service)
EXPOSE 5003

# Run app
CMD ["python", "payment_service.py"]
