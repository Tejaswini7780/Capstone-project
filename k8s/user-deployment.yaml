apiVersion: apps/v1
kind: Deployment
metadata:
  name: tejaswini-capstone-app-user-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user
  template:
    metadata:
      labels:
        app: user
    spec:
      containers:
      - name: user
        image: us-south1-docker.pkg.dev/citric-kit-489316-g6/teja-repo/user-service:latest
        ports:
        - containerPort: 5001
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector:
    app: user
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP